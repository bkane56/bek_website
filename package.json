{
  "name": "bek_website",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "next": "16.2.4"
  },
  "scripts": {
    "dev": "yarn --cwd frontend dev",
    "build": "yarn --cwd frontend build",
    "start": "yarn --cwd frontend start",
    "lint": "yarn --cwd frontend lint",
    "test": "yarn --cwd frontend test",
    "test:coverage": "yarn --cwd frontend test:coverage"
  }
}
